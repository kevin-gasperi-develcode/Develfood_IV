import styled from 'styled-components/native'

export const Container = styled.View`
  align-items: 'center';
`
export const ImageButton = styled.Image`
  /* tint-Color: focused ? 'red' : '#DEDCDC', */
  /* width: focused ? 27 : 25,
          height: focused ? 27 : 25, */
`
